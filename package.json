{
  "name": "team6_suite8",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --project=setup --project=bdd",
    "test:home": "npx playwright test --project=setup --project=bdd --grep 'Home Page'",
    "test:login": "npx playwright test --project=bdd --grep 'Login'",
    "test:headed": "npx playwright test --project=setup --project=bdd --headed",
    "test:home:headed": "npx playwright test --project=setup --project=bdd --headed --grep 'Home Page'",
    "report": "npx playwright show-report",
    "bddgen": "npx bddgen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "playwright-bdd": "^8.5.0"
  },
  "dependencies": {
    "winston": "^3.19.0"
  }
}
